# Dockerfile de DESENVOLVIMENTO — usado por docker-compose.dev.yml
# Apenas fornece o runtime Node; o código entra via volume e o
# `ng serve` roda em modo watch (hot-reload). NÃO usar em produção.
FROM node:22-alpine
WORKDIR /app
EXPOSE 80
